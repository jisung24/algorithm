/**
 * 
 * @param {*} n : 반 총 인원 수
 * @param {*} lost : 체육복을 안 가져온 학생(배열로 학생 숫자)
 * @param {*} reserve : 체육복을 ❗️빌려줄 수 있는❗️ 학생
 * ==> 내 것만 있는 학생도 있는데, 그 학생은 체육복을 빌려줄 수 없음.
 */
let solution = (n, lost, reserve) => {
    // Algorithm : 왜 그리디일까.. => 최대한 많은 수의 학생들을 체육수업을 듣게 하기 위함..? 
    for(let value of reserve) {
        if(value)
    }
}
console.log(5, [2,4], [1, 3, 5]);